if-shell '[ "#{window_panes}" -eq 1 ]' \
  'split-window -h \; split-window -v \; select-pane -L \; split-window -v \; select-pane -U \; split-window -h \; select-pane -R \; split-window -h \; select-pane -D \; split-window -h \; select-pane -L \; select-pane -L \; split-window -h \; select-layout tiled \; select-pane -t 0' \
  'display-message "8-pane macro expects a single pane window"'
