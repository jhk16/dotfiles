if-shell '[ "#{window_panes}" -eq 1 ]' \
  'split-window -h \; split-window -v \; select-pane -L \; split-window -v \; select-layout tiled \; select-pane -t 0' \
  'display-message "4-pane macro expects a single pane window"'
